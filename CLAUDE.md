# FlowRev — Claude Code 開発ルール

## 技術構成

| カテゴリ | 技術 |
|---|---|
| フレームワーク | Next.js 15 (App Router) |
| 言語 | TypeScript |
| スタイリング | Tailwind CSS |
| UIコンポーネント | shadcn/ui |
| バックエンド | Supabase (@supabase/ssr) |
| ディレクトリ構成 | `src/` |

---

## ファイル設計ルール

### 基本方針

- **1ファイル1責務**を守る
- **300行を超えそうなファイルは分割を提案**する
- **500行以上のファイルは作らない**
- UI・ロジック・型定義・DB処理を分ける
- ページファイル (`page.tsx`) には表示の組み立てだけを書く
- API処理は `app/api/` または `lib/` 配下に分離する
- 共通UIは `components/` 配下に分離する
- 型定義は `types/` 配下に分離する
- Supabase処理は `lib/supabase/` 配下にまとめる
- 同じ処理を複数箇所に書かない
- 既存ファイルに追記しすぎず、必要なら新規ファイルを作成する

### 禁止事項

- `page.tsx` に大量の処理を書く
- 1つのコンポーネントにフォーム・API通信・状態管理・表示ロジックを全部入れる
- 500行以上のファイルを作る
- 既存コードを確認せずに重複ファイルを作る
- とりあえず1ファイルに全部入れる

---

## ディレクトリ構成規約

```
src/
├── app/
│   ├── (auth)/              # 認証関連ページグループ
│   ├── api/                 # Route Handlers
│   └── dashboard/           # 管理画面
├── components/
│   ├── auth/                # 認証UI
│   ├── layout/              # レイアウト共通コンポーネント
│   └── ui/                  # shadcn/ui コンポーネント
├── lib/
│   ├── supabase/
│   │   ├── client.ts        # ブラウザ用 Supabase クライアント
│   │   └── server.ts        # サーバー用 Supabase クライアント
│   └── utils.ts             # 汎用ユーティリティ (cn など)
└── types/                   # 型定義 (.ts ファイルのみ)
```

---

## 実装前チェックリスト

1. 変更対象ファイルを確認する
2. 既存機能との重複を確認する
3. 追加するファイル構成を提示する
4. 300行を超えそうな場合は分割案を出す

## 実装後チェックリスト

1. 変更ファイル一覧を出す
2. 各ファイルの役割を説明する
3. 肥大化リスクがあるファイルを報告する
4. `npm run build` を実行して成功を確認する
